{
  "name": "tutorial_09_merge_report_lab",
  "version": "1.0.0",
  "private": true,
  "description": "§15 Reporters: shard with the blob reporter, then merge-reports into one HTML",
  "type": "module",
  "scripts": {
    "test": "taqwright test",
    "clean": "rm -rf blob-report all-blobs playwright-report",
    "shard1": "taqwright test --shard 1/2 --reporter blob && mkdir -p all-blobs && mv blob-report/report-*.zip all-blobs/",
    "shard2": "taqwright test --shard 2/2 --reporter blob && mkdir -p all-blobs && mv blob-report/report-*.zip all-blobs/",
    "merge": "taqwright merge-reports all-blobs --reporter html",
    "report": "taqwright show-report",
    "doctor": "taqwright doctor",
    "devices": "taqwright devices",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "taqwright": "npm:@taqwright/taqwright@^1.0.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
